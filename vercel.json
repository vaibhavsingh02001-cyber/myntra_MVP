{
  "version": 2,
  "installCommand": "echo 'No install step required'",
  "buildCommand": "echo 'No build step required'",
  "builds": [
    {
      "src": "frontend/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/frontend/$1"
    }
  ]
}
